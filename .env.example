# Supabase Configuration
SUPABASE_URL=https://xxx.supabase.co
SUPABASE_JWKS_URL=https://xxx.supabase.co/auth/v1/jwks
SUPABASE_JWT_SECRET=your-jwt-secret
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key

# Supabase Edge Function API Key (used for external-api-proxy)
# This is passed to SupabaseClient constructor
SUPABASE_EDGE_FUNCTION_API_KEY=hikvision-sync-2024

# Event Function Configuration (for pontaj-adauga-eveniment)
# These are currently hardcoded and need to be moved to env vars
SUPABASE_EVENT_FUNCTION_URL=https://xxx.supabase.co/functions/v1/pontaj-adauga-eveniment
SUPABASE_EVENT_FUNCTION_API_KEY=your-event-api-key

# CORS Configuration
# Comma-separated list of allowed origins
ALLOWED_ORIGINS=http://localhost:3000

# VPN/IP Whitelist for Device Events
# VPN subnet that devices will connect from (e.g., 10.100.0.0/24)
VPN_SUBNET=10.100.0.0/24

# Optional: Additional allowed IPs for device events (comma-separated)
# Format: CIDR ranges or single IPs
# ALLOWED_EVENT_IPS=10.100.0.0/24,192.168.1.0/24

# Optional: Rate Limiting Configuration
# RATE_LIMIT_ENABLED=true